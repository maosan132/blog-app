<h1>All Tags</h1>

<ul id="tags">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
      <button style ="padding:5px"><%= link_to " Delete [x]", tag_path(@tag) , method: :delete, data: {confirm: "Really delete the Tag?"} %></button>
    </li>
  <% end %>
</ul>